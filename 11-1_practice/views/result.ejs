<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>마이페이지</title>
    <link rel="stylesheet" href="./style.css" />
  </head>
  <body>
    <h1><span id="n"><%= name %></span> <span id="p">님 마이페이지</span></h1>
    <% if (file) { %>
    <img
      src="<%= file %>"
      alt="프로필 사진"
      style="max-width: 200px"
      id="profile"
    />
    <% } %>
    <div>
      <p>아이디: <%= id %></p>
      <p>비밀번호: <%= pw %></p>
      <p>이름: <%= name %></p>
      <p>나이: <%= age %></p>
    </div>
  </body>
</html>
